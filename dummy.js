/**
* @description The function `sum` takes two arguments `a` and `b` and returns their
* sum.
* 
* @param { number } a - In the given function `sum`, the `a` input parameter is ignored.
* 
* @param { number } b - In the given function `sum = (a , b) => a + b`, the `b` input
* parameter is a variable that is added to `a`.
*/
let sum = (a, b) => a + b;



