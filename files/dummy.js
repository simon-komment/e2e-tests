/**
* @description The function `sum` takes two arguments `a` and `b`, and returns their
* sum.
* 
* @param { number } a - In the function `sum = (a , b) => a + b`, the `a` input
* parameter is ignored.
* 
* @param { number } b - The `b` input parameter adds its value to the result of the
* expression `a`.
*/
let sum = (a, b) => a + b;



















































